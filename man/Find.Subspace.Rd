% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PaletteSC.R
\name{Find.Subspace}
\alias{Find.Subspace}
\title{Find subspace embeddings across modalities}
\usage{
Find.Subspace(
  object,
  modal = NULL,
  sample = NULL,
  lambda = c(0.8),
  dim.reduce = TRUE,
  sub.dims = list(1:40),
  cos.dims = list(1:50),
  Angle.var = 15,
  max.Angle = 50,
  joint = FALSE,
  supervised = FALSE,
  proj.scale = TRUE,
  proj.center = TRUE,
  L2.norm = FALSE,
  seed = 123L,
  pre.reduce = FALSE,
  pre_dims = list(1:100),
  RP_thresh = 10000
)
}
\arguments{
\item{object}{An analysis object containing assay data, metadata, and representative cells.}

\item{modal}{A character vector specifying modalities to process (default: all in object).}

\item{sample}{A character vector specifying which sample(s) to include (default: all in object).}

\item{lambda}{A numeric vector of regularization parameters, one per modality (default: 0.9).}

\item{dim.reduce}{Logical; whether to perform dimensionality reduction before subspace learning (default: TRUE).}

\item{sub.dims}{A list of integer vectors specifying which subspace dimensions to retain per modality.}

\item{cos.dims}{A list of integer vectors specifying dimensions used in cosine kernel computation.}

\item{Angle.var}{A numeric vector controlling angular variance in kernel construction (default: 10).}

\item{max.Angle}{A numeric vector specifying the maximum angle threshold for kernel construction (default: 35).}

\item{joint}{Logical; whether to compute joint subspaces across batches (default: TRUE).}

\item{supervised}{Logical; whether to run supervised version using \code{Find_SubSpace_s} (default: FALSE).}

\item{proj.scale}{Logical; whether to scale features during projection (default: TRUE).}

\item{proj.center}{Logical; whether to center features during projection (default: TRUE).}

\item{L2.norm}{Logical; whether to apply L2 normalization to the final embedding (default: TRUE).}

\item{seed}{Integer random seed for reproducibility (default: 123L).}

\item{pre.reduce}{Logical; whether to perform pre-dimensionality reduction before Bi-sPCA (default: FALSE).}

\item{pre_dims}{List of integer vectors specifying dimensions to keep in pre-reduction (default: list(1:3000)).}

\item{RP_thresh}{Integer; number of rows above which random projection is applied (default: 10000).}
}
\value{
The input object with updated subspace embeddings stored in each assay.
}
\description{
\code{Find.Subspace} computes subspace embeddings for one or more modalities in the input object.
Depending on the \code{supervised} flag, it will call either the supervised version
or the unsupervised version function. The resulting embeddings are stored in the corresponding assays.
}
